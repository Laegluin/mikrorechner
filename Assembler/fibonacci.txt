#die n-te fibonacci zahl berechnen, bekommt n über R0 übergeben, output in R31
#die 0-te ficonacci zahl wäre hier 0

R1 = 1                      #zum dekrementieren und inkrementieren
R2 = 1                      #summand
R31 = 0                     #output

compare R0 = null           _schleife
jump_if to ende            #sobald in R0 0 steht ist es zuende
R0 = R0 - R1               #R0 dekrementieren
R31 = R31 + R2
R2 = R2 + R1               #summand inkrementieren
jump to schleife

halt                        _ende